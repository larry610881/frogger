# Changesets

This folder is managed by [@changesets/cli](https://github.com/changesets/changesets).

It holds `.md` files describing package changes that haven't been released yet.
Run `pnpm changeset` to create a new changeset.
